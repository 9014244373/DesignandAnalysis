#include<stdio.h>
int main() {
	int fact=1;
	int  i, n;
	printf("Enter the N value: ");
	scanf("%d",&n);
	if(n>0){
	for(i=1;i<=n;++i){
		fact = fact*i;
	}
    }
  
	printf("The Factorial of %d is: %d",n,fact);
	return 0;
}

